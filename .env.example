# Transak API Configuration
# Get API key + API secret from: https://partner.transak.com/
# Select "Staging" in the dashboard, then Developers → copy both values
VITE_TRANSAK_API_KEY=your_staging_api_key
TRANSAK_API_KEY=your_staging_api_key
TRANSAK_API_SECRET=your_staging_api_secret
VITE_TRANSAK_ENV=staging
TRANSAK_ENV=staging
# Options: staging (for testing) or production (for live transactions)
