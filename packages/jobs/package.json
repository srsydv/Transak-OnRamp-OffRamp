{
  "name": "@payment-gateway/jobs",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@payment-gateway/observability": "0.1.0",
    "@payment-gateway/persistence": "0.1.0",
    "bullmq": "^5.40.5",
    "ioredis": "^5.4.2"
  }
}
